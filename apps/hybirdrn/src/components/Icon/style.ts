import { StyleSheet } from 'react-native';
import { theme } from '../../styles/theme';

export const defaultIconStyles = StyleSheet.create({
  container: {
    color: theme.colorText,
    fontSize: theme.fontSizeBase,
  },
});

export default defaultIconStyles;
