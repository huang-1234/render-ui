{
  "name": "@cross-platform/example",
  "version": "1.0.0",
  "description": "跨端框架示例应用",
  "main": "src/index.js",
  "scripts": {
    "dev": "cross-cli dev",
    "dev:h5": "cross-cli dev -p h5",
    "dev:rn": "cross-cli dev -p rn",
    "dev:weapp": "cross-cli dev -p weapp",
    "build": "cross-cli build",
    "build:h5": "cross-cli build -p h5",
    "build:rn": "cross-cli build -p rn",
    "build:weapp": "cross-cli build -p weapp",
    "build:all": "cross-cli build -p all",
    "test": "vitest"
  },
  "keywords": ["cross-platform", "example", "demo"],
  "author": "",
  "license": "MIT",
  "dependencies": {
    "@cross-platform/core": "workspace:*",
    "@cross-platform/components": "workspace:*",
    "react": "^18.0.0",
    "react-dom": "^18.0.0"
  },
  "devDependencies": {
    "@cross-platform/cli": "workspace:*",
    "@types/react": "^18.0.0",
    "@types/react-dom": "^18.0.0",
    "typescript": "^5.0.0",
    "vite": "^5.0.0",
    "vitest": "^1.0.0"
  }
}